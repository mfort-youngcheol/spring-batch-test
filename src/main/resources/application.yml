spring:
  datasource:
    url: jdbc:mysql://localhost:3306/playground
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  batch:
    jdbc:
      initialize-schema: always
    job:
      names: secondJob # run only Job with name `secondJob`
